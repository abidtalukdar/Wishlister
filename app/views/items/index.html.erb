<h1>All Items</h1>

<% @items.each do |item| %>
    <ul>
        <li>
            <%= link_to item.name, item_path(item) %>
        </li>
    </ul>
<% end %>

<%= button_to "View My Wishlists", wishlists_path, method: :get %>